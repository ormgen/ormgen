{
	"name": "@ormgen/test.ecom",
	"license": "MIT",
	"scripts": {
		"test:types": "tsc --noEmit",
		"test:types:watch": "tsc --noEmit --watch",
		"ecom": "DATABASE_URL=postgres://test tsx ecom/src/index.ts",
		"ecom:sync": "npm run ecom -- --sync",
		"ecom:seed": "npm run ecom -- --seed"
	},
	"dependencies": {
		"ormgen": "^0.0.8"
	},
	"devDependencies": {
		"tsx": "^4.7.1",
		"typescript": "^5.4.2"
	}
}
